import { createTheme } from "@mui/material";

const ThemeDefault = createTheme({
  palette: {
    primary: {
      main: "#3F1C32",
    },
    secondary: {
      main: "#005D66",
    },
  },
});

export default ThemeDefault;
